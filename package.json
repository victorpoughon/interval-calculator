{
  "name": "interval-union-calculator",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "generate": "ohm generateBundles --esm --withTypes 'src/*.ohm'",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node --import tsx --test tests/*"
  },
  "devDependencies": {
    "@ohm-js/cli": "^2.0.1",
    "@types/node": "^24.6.2",
    "tsx": "^4.20.6",
    "typescript": "~5.8.3",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "not-so-float": "^2.0.0",
    "ohm-js": "^17.2.1",
    "ts-pattern": "^5.8.0"
  }
}
